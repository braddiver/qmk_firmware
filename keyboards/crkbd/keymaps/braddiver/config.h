#pragma once

// Wait before triggering mod version of mod-tap keys
#define TAPPING_TERM 200
// But define which mod keys need to wait (like Shift). For others, quick typing uses the mod (like App switching with Cmd-TAB).
#define HOLD_ON_OTHER_KEY_PRESS_PER_KEY
